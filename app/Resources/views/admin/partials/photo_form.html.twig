<div class="col-sm-6 col-md-4 col-lg-3">
    {{ form_start(form) }}
    {{ form_row(form.title) }}
    {{ form_row(form.description) }}
    {{ form_widget(form.file) }}
    {{ form_end(form) }}
    <hr>
</div>
